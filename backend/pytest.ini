[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -m "not benchmark"
markers =
    benchmark: real-OpenAI linguistic benchmark (costs money, slow) — run explicitly with `pytest -m benchmark tests/benchmark`
